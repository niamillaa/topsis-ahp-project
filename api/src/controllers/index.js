const alternatives = require('./controller-alternatives');
const criteria = require('./controller-criterias');
const model = require('./controller-model');

module.exports = {
  alternatives,
  criteria,
  model,
};